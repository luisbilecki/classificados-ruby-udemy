//alert("<%= @admin.email %>");
//garante que a página vai ser carregada por completo
$(function(){
  let $sendMailModal = $('#sendMailModal');

  // Setting data on Form
  $('#recipient-text').val('<%= @admin.email %>');
  $('h4.modal-title').text('Enviar mensagem para ' + '<%= @admin.name %>');

  // Shows modal
  $sendMailModal.modal();

  // Set focus on 'Subject' after modal is shown
  $sendMailModal.on('shown.bs.modal', function(){
      $('#subject-text').focus();
  })

});
