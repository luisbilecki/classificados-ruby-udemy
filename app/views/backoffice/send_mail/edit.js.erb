//alert("<%= @admin.email %>");
//garante que a página vai ser carregada por completo
$(function(){
  $('#recipient-name').val('<%= @admin.email %>');
  $('h4.modal-title').text('Enviar mensagem para ' + '<%= @admin.name %>');
  $('#sendMailModal').modal();
  $('#sendMailModal').on('shown.bs.modal', function(){
      $('#message-text').focus();
  })

});
